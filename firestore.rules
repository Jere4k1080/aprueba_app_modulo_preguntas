rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // La app usa la API. Firebase Admin accede con IAM e ignora estas reglas.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
